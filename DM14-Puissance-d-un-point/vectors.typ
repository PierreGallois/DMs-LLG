#let AB = math.class("normal", $arrow(A B)$)
#let AC = math.class("normal", $arrow(A C)$)
#let AD = math.class("normal", $arrow(A D)$)
#let AE = math.class("normal", $arrow(A E)$)
#let AF = math.class("normal", $arrow(A F)$)
#let AG = math.class("normal", $arrow(A G)$)
#let AH = math.class("normal", $arrow(A H)$)
#let AI = math.class("normal", $arrow(A I)$)
#let AJ = math.class("normal", $arrow(A J)$)
#let AK = math.class("normal", $arrow(A K)$)
#let AL = math.class("normal", $arrow(A L)$)
#let AM = math.class("normal", $arrow(A M)$)
#let AN = math.class("normal", $arrow(A N)$)
#let AO = math.class("normal", $arrow(A O)$)
#let AP = math.class("normal", $arrow(A P)$)
#let AQ = math.class("normal", $arrow(A Q)$)
#let AR = math.class("normal", $arrow(A R)$)
#let AS = math.class("normal", $arrow(A S)$)
#let AT = math.class("normal", $arrow(A T)$)
#let AU = math.class("normal", $arrow(A U)$)
#let AV = math.class("normal", $arrow(A V)$)
#let AW = math.class("normal", $arrow(A W)$)
#let AX = math.class("normal", $arrow(A X)$)
#let AY = math.class("normal", $arrow(A Y)$)
#let AZ = math.class("normal", $arrow(A Z)$)
#let BA = math.class("normal", $arrow(B A)$)
#let BC = math.class("normal", $arrow(B C)$)
#let BD = math.class("normal", $arrow(B D)$)
#let BE = math.class("normal", $arrow(B E)$)
#let BF = math.class("normal", $arrow(B F)$)
#let BG = math.class("normal", $arrow(B G)$)
#let BH = math.class("normal", $arrow(B H)$)
#let BI = math.class("normal", $arrow(B I)$)
#let BJ = math.class("normal", $arrow(B J)$)
#let BK = math.class("normal", $arrow(B K)$)
#let BL = math.class("normal", $arrow(B L)$)
#let BM = math.class("normal", $arrow(B M)$)
#let BN = math.class("normal", $arrow(B N)$)
#let BO = math.class("normal", $arrow(B O)$)
#let BP = math.class("normal", $arrow(B P)$)
#let BQ = math.class("normal", $arrow(B Q)$)
#let BR = math.class("normal", $arrow(B R)$)
#let BS = math.class("normal", $arrow(B S)$)
#let BT = math.class("normal", $arrow(B T)$)
#let BU = math.class("normal", $arrow(B U)$)
#let BV = math.class("normal", $arrow(B V)$)
#let BW = math.class("normal", $arrow(B W)$)
#let BX = math.class("normal", $arrow(B X)$)
#let BY = math.class("normal", $arrow(B Y)$)
#let BZ = math.class("normal", $arrow(B Z)$)
#let CA = math.class("normal", $arrow(C A)$)
#let CB = math.class("normal", $arrow(C B)$)
#let CD = math.class("normal", $arrow(C D)$)
#let CE = math.class("normal", $arrow(C E)$)
#let CF = math.class("normal", $arrow(C F)$)
#let CG = math.class("normal", $arrow(C G)$)
#let CH = math.class("normal", $arrow(C H)$)
#let CI = math.class("normal", $arrow(C I)$)
#let CJ = math.class("normal", $arrow(C J)$)
#let CK = math.class("normal", $arrow(C K)$)
#let CL = math.class("normal", $arrow(C L)$)
#let CM = math.class("normal", $arrow(C M)$)
#let CN = math.class("normal", $arrow(C N)$)
#let CO = math.class("normal", $arrow(C O)$)
#let CP = math.class("normal", $arrow(C P)$)
#let CQ = math.class("normal", $arrow(C Q)$)
#let CR = math.class("normal", $arrow(C R)$)
#let CS = math.class("normal", $arrow(C S)$)
#let CT = math.class("normal", $arrow(C T)$)
#let CU = math.class("normal", $arrow(C U)$)
#let CV = math.class("normal", $arrow(C V)$)
#let CW = math.class("normal", $arrow(C W)$)
#let CX = math.class("normal", $arrow(C X)$)
#let CY = math.class("normal", $arrow(C Y)$)
#let CZ = math.class("normal", $arrow(C Z)$)
#let DA = math.class("normal", $arrow(D A)$)
#let DB = math.class("normal", $arrow(D B)$)
#let DC = math.class("normal", $arrow(D C)$)
#let DE = math.class("normal", $arrow(D E)$)
#let DF = math.class("normal", $arrow(D F)$)
#let DG = math.class("normal", $arrow(D G)$)
#let DH = math.class("normal", $arrow(D H)$)
#let DI = math.class("normal", $arrow(D I)$)
#let DJ = math.class("normal", $arrow(D J)$)
#let DK = math.class("normal", $arrow(D K)$)
#let DL = math.class("normal", $arrow(D L)$)
#let DM = math.class("normal", $arrow(D M)$)
#let DN = math.class("normal", $arrow(D N)$)
#let DO = math.class("normal", $arrow(D O)$)
#let DP = math.class("normal", $arrow(D P)$)
#let DQ = math.class("normal", $arrow(D Q)$)
#let DR = math.class("normal", $arrow(D R)$)
#let DS = math.class("normal", $arrow(D S)$)
#let DT = math.class("normal", $arrow(D T)$)
#let DU = math.class("normal", $arrow(D U)$)
#let DV = math.class("normal", $arrow(D V)$)
#let DW = math.class("normal", $arrow(D W)$)
#let DX = math.class("normal", $arrow(D X)$)
#let DY = math.class("normal", $arrow(D Y)$)
#let DZ = math.class("normal", $arrow(D Z)$)
#let EA = math.class("normal", $arrow(E A)$)
#let EB = math.class("normal", $arrow(E B)$)
#let EC = math.class("normal", $arrow(E C)$)
#let ED = math.class("normal", $arrow(E D)$)
#let EF = math.class("normal", $arrow(E F)$)
#let EG = math.class("normal", $arrow(E G)$)
#let EH = math.class("normal", $arrow(E H)$)
#let EI = math.class("normal", $arrow(E I)$)
#let EJ = math.class("normal", $arrow(E J)$)
#let EK = math.class("normal", $arrow(E K)$)
#let EL = math.class("normal", $arrow(E L)$)
#let EM = math.class("normal", $arrow(E M)$)
#let EN = math.class("normal", $arrow(E N)$)
#let EO = math.class("normal", $arrow(E O)$)
#let EP = math.class("normal", $arrow(E P)$)
#let EQ = math.class("normal", $arrow(E Q)$)
#let ER = math.class("normal", $arrow(E R)$)
#let ES = math.class("normal", $arrow(E S)$)
#let ET = math.class("normal", $arrow(E T)$)
#let EU = math.class("normal", $arrow(E U)$)
#let EV = math.class("normal", $arrow(E V)$)
#let EW = math.class("normal", $arrow(E W)$)
#let EX = math.class("normal", $arrow(E X)$)
#let EY = math.class("normal", $arrow(E Y)$)
#let EZ = math.class("normal", $arrow(E Z)$)
#let FA = math.class("normal", $arrow(F A)$)
#let FB = math.class("normal", $arrow(F B)$)
#let FC = math.class("normal", $arrow(F C)$)
#let FD = math.class("normal", $arrow(F D)$)
#let FE = math.class("normal", $arrow(F E)$)
#let FG = math.class("normal", $arrow(F G)$)
#let FH = math.class("normal", $arrow(F H)$)
#let FI = math.class("normal", $arrow(F I)$)
#let FJ = math.class("normal", $arrow(F J)$)
#let FK = math.class("normal", $arrow(F K)$)
#let FL = math.class("normal", $arrow(F L)$)
#let FM = math.class("normal", $arrow(F M)$)
#let FN = math.class("normal", $arrow(F N)$)
#let FO = math.class("normal", $arrow(F O)$)
#let FP = math.class("normal", $arrow(F P)$)
#let FQ = math.class("normal", $arrow(F Q)$)
#let FR = math.class("normal", $arrow(F R)$)
#let FS = math.class("normal", $arrow(F S)$)
#let FT = math.class("normal", $arrow(F T)$)
#let FU = math.class("normal", $arrow(F U)$)
#let FV = math.class("normal", $arrow(F V)$)
#let FW = math.class("normal", $arrow(F W)$)
#let FX = math.class("normal", $arrow(F X)$)
#let FY = math.class("normal", $arrow(F Y)$)
#let FZ = math.class("normal", $arrow(F Z)$)
#let GA = math.class("normal", $arrow(G A)$)
#let GB = math.class("normal", $arrow(G B)$)
#let GC = math.class("normal", $arrow(G C)$)
#let GD = math.class("normal", $arrow(G D)$)
#let GE = math.class("normal", $arrow(G E)$)
#let GF = math.class("normal", $arrow(G F)$)
#let GH = math.class("normal", $arrow(G H)$)
#let GI = math.class("normal", $arrow(G I)$)
#let GJ = math.class("normal", $arrow(G J)$)
#let GK = math.class("normal", $arrow(G K)$)
#let GL = math.class("normal", $arrow(G L)$)
#let GM = math.class("normal", $arrow(G M)$)
#let GN = math.class("normal", $arrow(G N)$)
#let GO = math.class("normal", $arrow(G O)$)
#let GP = math.class("normal", $arrow(G P)$)
#let GQ = math.class("normal", $arrow(G Q)$)
#let GR = math.class("normal", $arrow(G R)$)
#let GS = math.class("normal", $arrow(G S)$)
#let GT = math.class("normal", $arrow(G T)$)
#let GU = math.class("normal", $arrow(G U)$)
#let GV = math.class("normal", $arrow(G V)$)
#let GW = math.class("normal", $arrow(G W)$)
#let GX = math.class("normal", $arrow(G X)$)
#let GY = math.class("normal", $arrow(G Y)$)
#let GZ = math.class("normal", $arrow(G Z)$)
#let HA = math.class("normal", $arrow(H A)$)
#let HB = math.class("normal", $arrow(H B)$)
#let HC = math.class("normal", $arrow(H C)$)
#let HD = math.class("normal", $arrow(H D)$)
#let HE = math.class("normal", $arrow(H E)$)
#let HF = math.class("normal", $arrow(H F)$)
#let HG = math.class("normal", $arrow(H G)$)
#let HI = math.class("normal", $arrow(H I)$)
#let HJ = math.class("normal", $arrow(H J)$)
#let HK = math.class("normal", $arrow(H K)$)
#let HL = math.class("normal", $arrow(H L)$)
#let HM = math.class("normal", $arrow(H M)$)
#let HN = math.class("normal", $arrow(H N)$)
#let HO = math.class("normal", $arrow(H O)$)
#let HP = math.class("normal", $arrow(H P)$)
#let HQ = math.class("normal", $arrow(H Q)$)
#let HR = math.class("normal", $arrow(H R)$)
#let HS = math.class("normal", $arrow(H S)$)
#let HT = math.class("normal", $arrow(H T)$)
#let HU = math.class("normal", $arrow(H U)$)
#let HV = math.class("normal", $arrow(H V)$)
#let HW = math.class("normal", $arrow(H W)$)
#let HX = math.class("normal", $arrow(H X)$)
#let HY = math.class("normal", $arrow(H Y)$)
#let HZ = math.class("normal", $arrow(H Z)$)
#let IA = math.class("normal", $arrow(I A)$)
#let IB = math.class("normal", $arrow(I B)$)
#let IC = math.class("normal", $arrow(I C)$)
#let ID = math.class("normal", $arrow(I D)$)
#let IE = math.class("normal", $arrow(I E)$)
#let IF = math.class("normal", $arrow(I F)$)
#let IG = math.class("normal", $arrow(I G)$)
#let IH = math.class("normal", $arrow(I H)$)
#let IJ = math.class("normal", $arrow(I J)$)
#let IK = math.class("normal", $arrow(I K)$)
#let IL = math.class("normal", $arrow(I L)$)
#let IM = math.class("normal", $arrow(I M)$)
#let IN = math.class("normal", $arrow(I N)$)
#let IO = math.class("normal", $arrow(I O)$)
#let IP = math.class("normal", $arrow(I P)$)
#let IQ = math.class("normal", $arrow(I Q)$)
#let IR = math.class("normal", $arrow(I R)$)
#let IS = math.class("normal", $arrow(I S)$)
#let IT = math.class("normal", $arrow(I T)$)
#let IU = math.class("normal", $arrow(I U)$)
#let IV = math.class("normal", $arrow(I V)$)
#let IW = math.class("normal", $arrow(I W)$)
#let IX = math.class("normal", $arrow(I X)$)
#let IY = math.class("normal", $arrow(I Y)$)
#let IZ = math.class("normal", $arrow(I Z)$)
#let JA = math.class("normal", $arrow(J A)$)
#let JB = math.class("normal", $arrow(J B)$)
#let JC = math.class("normal", $arrow(J C)$)
#let JD = math.class("normal", $arrow(J D)$)
#let JE = math.class("normal", $arrow(J E)$)
#let JF = math.class("normal", $arrow(J F)$)
#let JG = math.class("normal", $arrow(J G)$)
#let JH = math.class("normal", $arrow(J H)$)
#let JI = math.class("normal", $arrow(J I)$)
#let JK = math.class("normal", $arrow(J K)$)
#let JL = math.class("normal", $arrow(J L)$)
#let JM = math.class("normal", $arrow(J M)$)
#let JN = math.class("normal", $arrow(J N)$)
#let JO = math.class("normal", $arrow(J O)$)
#let JP = math.class("normal", $arrow(J P)$)
#let JQ = math.class("normal", $arrow(J Q)$)
#let JR = math.class("normal", $arrow(J R)$)
#let JS = math.class("normal", $arrow(J S)$)
#let JT = math.class("normal", $arrow(J T)$)
#let JU = math.class("normal", $arrow(J U)$)
#let JV = math.class("normal", $arrow(J V)$)
#let JW = math.class("normal", $arrow(J W)$)
#let JX = math.class("normal", $arrow(J X)$)
#let JY = math.class("normal", $arrow(J Y)$)
#let JZ = math.class("normal", $arrow(J Z)$)
#let KA = math.class("normal", $arrow(K A)$)
#let KB = math.class("normal", $arrow(K B)$)
#let KC = math.class("normal", $arrow(K C)$)
#let KD = math.class("normal", $arrow(K D)$)
#let KE = math.class("normal", $arrow(K E)$)
#let KF = math.class("normal", $arrow(K F)$)
#let KG = math.class("normal", $arrow(K G)$)
#let KH = math.class("normal", $arrow(K H)$)
#let KI = math.class("normal", $arrow(K I)$)
#let KJ = math.class("normal", $arrow(K J)$)
#let KL = math.class("normal", $arrow(K L)$)
#let KM = math.class("normal", $arrow(K M)$)
#let KN = math.class("normal", $arrow(K N)$)
#let KO = math.class("normal", $arrow(K O)$)
#let KP = math.class("normal", $arrow(K P)$)
#let KQ = math.class("normal", $arrow(K Q)$)
#let KR = math.class("normal", $arrow(K R)$)
#let KS = math.class("normal", $arrow(K S)$)
#let KT = math.class("normal", $arrow(K T)$)
#let KU = math.class("normal", $arrow(K U)$)
#let KV = math.class("normal", $arrow(K V)$)
#let KW = math.class("normal", $arrow(K W)$)
#let KX = math.class("normal", $arrow(K X)$)
#let KY = math.class("normal", $arrow(K Y)$)
#let KZ = math.class("normal", $arrow(K Z)$)
#let LA = math.class("normal", $arrow(L A)$)
#let LB = math.class("normal", $arrow(L B)$)
#let LC = math.class("normal", $arrow(L C)$)
#let LD = math.class("normal", $arrow(L D)$)
#let LE = math.class("normal", $arrow(L E)$)
#let LF = math.class("normal", $arrow(L F)$)
#let LG = math.class("normal", $arrow(L G)$)
#let LH = math.class("normal", $arrow(L H)$)
#let LI = math.class("normal", $arrow(L I)$)
#let LJ = math.class("normal", $arrow(L J)$)
#let LK = math.class("normal", $arrow(L K)$)
#let LM = math.class("normal", $arrow(L M)$)
#let LN = math.class("normal", $arrow(L N)$)
#let LO = math.class("normal", $arrow(L O)$)
#let LP = math.class("normal", $arrow(L P)$)
#let LQ = math.class("normal", $arrow(L Q)$)
#let LR = math.class("normal", $arrow(L R)$)
#let LS = math.class("normal", $arrow(L S)$)
#let LT = math.class("normal", $arrow(L T)$)
#let LU = math.class("normal", $arrow(L U)$)
#let LV = math.class("normal", $arrow(L V)$)
#let LW = math.class("normal", $arrow(L W)$)
#let LX = math.class("normal", $arrow(L X)$)
#let LY = math.class("normal", $arrow(L Y)$)
#let LZ = math.class("normal", $arrow(L Z)$)
#let MA = math.class("normal", $arrow(M A)$)
#let MB = math.class("normal", $arrow(M B)$)
#let MC = math.class("normal", $arrow(M C)$)
#let MD = math.class("normal", $arrow(M D)$)
#let ME = math.class("normal", $arrow(M E)$)
#let MF = math.class("normal", $arrow(M F)$)
#let MG = math.class("normal", $arrow(M G)$)
#let MH = math.class("normal", $arrow(M H)$)
#let MI = math.class("normal", $arrow(M I)$)
#let MJ = math.class("normal", $arrow(M J)$)
#let MK = math.class("normal", $arrow(M K)$)
#let ML = math.class("normal", $arrow(M L)$)
#let MN = math.class("normal", $arrow(M N)$)
#let MO = math.class("normal", $arrow(M O)$)
#let MP = math.class("normal", $arrow(M P)$)
#let MQ = math.class("normal", $arrow(M Q)$)
#let MR = math.class("normal", $arrow(M R)$)
#let MS = math.class("normal", $arrow(M S)$)
#let MT = math.class("normal", $arrow(M T)$)
#let MU = math.class("normal", $arrow(M U)$)
#let MV = math.class("normal", $arrow(M V)$)
#let MW = math.class("normal", $arrow(M W)$)
#let MX = math.class("normal", $arrow(M X)$)
#let MY = math.class("normal", $arrow(M Y)$)
#let MZ = math.class("normal", $arrow(M Z)$)
#let NA = math.class("normal", $arrow(N A)$)
#let NB = math.class("normal", $arrow(N B)$)
#let NC = math.class("normal", $arrow(N C)$)
#let ND = math.class("normal", $arrow(N D)$)
#let NE = math.class("normal", $arrow(N E)$)
#let NF = math.class("normal", $arrow(N F)$)
#let NG = math.class("normal", $arrow(N G)$)
#let NH = math.class("normal", $arrow(N H)$)
#let NI = math.class("normal", $arrow(N I)$)
#let NJ = math.class("normal", $arrow(N J)$)
#let NK = math.class("normal", $arrow(N K)$)
#let NL = math.class("normal", $arrow(N L)$)
#let NM = math.class("normal", $arrow(N M)$)
#let NO = math.class("normal", $arrow(N O)$)
#let NP = math.class("normal", $arrow(N P)$)
#let NQ = math.class("normal", $arrow(N Q)$)
#let NR = math.class("normal", $arrow(N R)$)
#let NS = math.class("normal", $arrow(N S)$)
#let NT = math.class("normal", $arrow(N T)$)
#let NU = math.class("normal", $arrow(N U)$)
#let NV = math.class("normal", $arrow(N V)$)
#let NW = math.class("normal", $arrow(N W)$)
#let NX = math.class("normal", $arrow(N X)$)
#let NY = math.class("normal", $arrow(N Y)$)
#let NZ = math.class("normal", $arrow(N Z)$)
#let OA = math.class("normal", $arrow(O A)$)
#let OB = math.class("normal", $arrow(O B)$)
#let OC = math.class("normal", $arrow(O C)$)
#let OD = math.class("normal", $arrow(O D)$)
#let OE = math.class("normal", $arrow(O E)$)
#let OF = math.class("normal", $arrow(O F)$)
#let OG = math.class("normal", $arrow(O G)$)
#let OH = math.class("normal", $arrow(O H)$)
#let OI = math.class("normal", $arrow(O I)$)
#let OJ = math.class("normal", $arrow(O J)$)
#let OK = math.class("normal", $arrow(O K)$)
#let OL = math.class("normal", $arrow(O L)$)
#let OM = math.class("normal", $arrow(O M)$)
#let ON = math.class("normal", $arrow(O N)$)
#let OP = math.class("normal", $arrow(O P)$)
#let OQ = math.class("normal", $arrow(O Q)$)
#let OR = math.class("normal", $arrow(O R)$)
#let OS = math.class("normal", $arrow(O S)$)
#let OT = math.class("normal", $arrow(O T)$)
#let OU = math.class("normal", $arrow(O U)$)
#let OV = math.class("normal", $arrow(O V)$)
#let OW = math.class("normal", $arrow(O W)$)
#let OX = math.class("normal", $arrow(O X)$)
#let OY = math.class("normal", $arrow(O Y)$)
#let OZ = math.class("normal", $arrow(O Z)$)
#let PA = math.class("normal", $arrow(P A)$)
#let PB = math.class("normal", $arrow(P B)$)
#let PC = math.class("normal", $arrow(P C)$)
#let PD = math.class("normal", $arrow(P D)$)
#let PE = math.class("normal", $arrow(P E)$)
#let PF = math.class("normal", $arrow(P F)$)
#let PG = math.class("normal", $arrow(P G)$)
#let PH = math.class("normal", $arrow(P H)$)
#let PI = math.class("normal", $arrow(P I)$)
#let PJ = math.class("normal", $arrow(P J)$)
#let PK = math.class("normal", $arrow(P K)$)
#let PL = math.class("normal", $arrow(P L)$)
#let PM = math.class("normal", $arrow(P M)$)
#let PN = math.class("normal", $arrow(P N)$)
#let PO = math.class("normal", $arrow(P O)$)
#let PQ = math.class("normal", $arrow(P Q)$)
#let PR = math.class("normal", $arrow(P R)$)
#let PS = math.class("normal", $arrow(P S)$)
#let PT = math.class("normal", $arrow(P T)$)
#let PU = math.class("normal", $arrow(P U)$)
#let PV = math.class("normal", $arrow(P V)$)
#let PW = math.class("normal", $arrow(P W)$)
#let PX = math.class("normal", $arrow(P X)$)
#let PY = math.class("normal", $arrow(P Y)$)
#let PZ = math.class("normal", $arrow(P Z)$)
#let QA = math.class("normal", $arrow(Q A)$)
#let QB = math.class("normal", $arrow(Q B)$)
#let QC = math.class("normal", $arrow(Q C)$)
#let QD = math.class("normal", $arrow(Q D)$)
#let QE = math.class("normal", $arrow(Q E)$)
#let QF = math.class("normal", $arrow(Q F)$)
#let QG = math.class("normal", $arrow(Q G)$)
#let QH = math.class("normal", $arrow(Q H)$)
#let QI = math.class("normal", $arrow(Q I)$)
#let QJ = math.class("normal", $arrow(Q J)$)
#let QK = math.class("normal", $arrow(Q K)$)
#let QL = math.class("normal", $arrow(Q L)$)
#let QM = math.class("normal", $arrow(Q M)$)
#let QN = math.class("normal", $arrow(Q N)$)
#let QO = math.class("normal", $arrow(Q O)$)
#let QP = math.class("normal", $arrow(Q P)$)
#let QR = math.class("normal", $arrow(Q R)$)
#let QS = math.class("normal", $arrow(Q S)$)
#let QT = math.class("normal", $arrow(Q T)$)
#let QU = math.class("normal", $arrow(Q U)$)
#let QV = math.class("normal", $arrow(Q V)$)
#let QW = math.class("normal", $arrow(Q W)$)
#let QX = math.class("normal", $arrow(Q X)$)
#let QY = math.class("normal", $arrow(Q Y)$)
#let QZ = math.class("normal", $arrow(Q Z)$)
#let RA = math.class("normal", $arrow(R A)$)
#let RB = math.class("normal", $arrow(R B)$)
#let RC = math.class("normal", $arrow(R C)$)
#let RD = math.class("normal", $arrow(R D)$)
#let RE = math.class("normal", $arrow(R E)$)
#let RF = math.class("normal", $arrow(R F)$)
#let RG = math.class("normal", $arrow(R G)$)
#let RH = math.class("normal", $arrow(R H)$)
#let RI = math.class("normal", $arrow(R I)$)
#let RJ = math.class("normal", $arrow(R J)$)
#let RK = math.class("normal", $arrow(R K)$)
#let RL = math.class("normal", $arrow(R L)$)
#let RM = math.class("normal", $arrow(R M)$)
#let RN = math.class("normal", $arrow(R N)$)
#let RO = math.class("normal", $arrow(R O)$)
#let RP = math.class("normal", $arrow(R P)$)
#let RQ = math.class("normal", $arrow(R Q)$)
#let RS = math.class("normal", $arrow(R S)$)
#let RT = math.class("normal", $arrow(R T)$)
#let RU = math.class("normal", $arrow(R U)$)
#let RV = math.class("normal", $arrow(R V)$)
#let RW = math.class("normal", $arrow(R W)$)
#let RX = math.class("normal", $arrow(R X)$)
#let RY = math.class("normal", $arrow(R Y)$)
#let RZ = math.class("normal", $arrow(R Z)$)
#let SA = math.class("normal", $arrow(S A)$)
#let SB = math.class("normal", $arrow(S B)$)
#let SC = math.class("normal", $arrow(S C)$)
#let SD = math.class("normal", $arrow(S D)$)
#let SE = math.class("normal", $arrow(S E)$)
#let SF = math.class("normal", $arrow(S F)$)
#let SG = math.class("normal", $arrow(S G)$)
#let SH = math.class("normal", $arrow(S H)$)
#let SI = math.class("normal", $arrow(S I)$)
#let SJ = math.class("normal", $arrow(S J)$)
#let SK = math.class("normal", $arrow(S K)$)
#let SL = math.class("normal", $arrow(S L)$)
#let SM = math.class("normal", $arrow(S M)$)
#let SN = math.class("normal", $arrow(S N)$)
#let SO = math.class("normal", $arrow(S O)$)
#let SP = math.class("normal", $arrow(S P)$)
#let SQ = math.class("normal", $arrow(S Q)$)
#let SR = math.class("normal", $arrow(S R)$)
#let ST = math.class("normal", $arrow(S T)$)
#let SU = math.class("normal", $arrow(S U)$)
#let SV = math.class("normal", $arrow(S V)$)
#let SW = math.class("normal", $arrow(S W)$)
#let SX = math.class("normal", $arrow(S X)$)
#let SY = math.class("normal", $arrow(S Y)$)
#let SZ = math.class("normal", $arrow(S Z)$)
#let TA = math.class("normal", $arrow(T A)$)
#let TB = math.class("normal", $arrow(T B)$)
#let TC = math.class("normal", $arrow(T C)$)
#let TD = math.class("normal", $arrow(T D)$)
#let TE = math.class("normal", $arrow(T E)$)
#let TF = math.class("normal", $arrow(T F)$)
#let TG = math.class("normal", $arrow(T G)$)
#let TH = math.class("normal", $arrow(T H)$)
#let TI = math.class("normal", $arrow(T I)$)
#let TJ = math.class("normal", $arrow(T J)$)
#let TK = math.class("normal", $arrow(T K)$)
#let TL = math.class("normal", $arrow(T L)$)
#let TM = math.class("normal", $arrow(T M)$)
#let TN = math.class("normal", $arrow(T N)$)
#let TO = math.class("normal", $arrow(T O)$)
#let TP = math.class("normal", $arrow(T P)$)
#let TQ = math.class("normal", $arrow(T Q)$)
#let TR = math.class("normal", $arrow(T R)$)
#let TS = math.class("normal", $arrow(T S)$)
#let TU = math.class("normal", $arrow(T U)$)
#let TV = math.class("normal", $arrow(T V)$)
#let TW = math.class("normal", $arrow(T W)$)
#let TX = math.class("normal", $arrow(T X)$)
#let TY = math.class("normal", $arrow(T Y)$)
#let TZ = math.class("normal", $arrow(T Z)$)
#let UA = math.class("normal", $arrow(U A)$)
#let UB = math.class("normal", $arrow(U B)$)
#let UC = math.class("normal", $arrow(U C)$)
#let UD = math.class("normal", $arrow(U D)$)
#let UE = math.class("normal", $arrow(U E)$)
#let UF = math.class("normal", $arrow(U F)$)
#let UG = math.class("normal", $arrow(U G)$)
#let UH = math.class("normal", $arrow(U H)$)
#let UI = math.class("normal", $arrow(U I)$)
#let UJ = math.class("normal", $arrow(U J)$)
#let UK = math.class("normal", $arrow(U K)$)
#let UL = math.class("normal", $arrow(U L)$)
#let UM = math.class("normal", $arrow(U M)$)
#let UN = math.class("normal", $arrow(U N)$)
#let UO = math.class("normal", $arrow(U O)$)
#let UP = math.class("normal", $arrow(U P)$)
#let UQ = math.class("normal", $arrow(U Q)$)
#let UR = math.class("normal", $arrow(U R)$)
#let US = math.class("normal", $arrow(U S)$)
#let UT = math.class("normal", $arrow(U T)$)
#let UV = math.class("normal", $arrow(U V)$)
#let UW = math.class("normal", $arrow(U W)$)
#let UX = math.class("normal", $arrow(U X)$)
#let UY = math.class("normal", $arrow(U Y)$)
#let UZ = math.class("normal", $arrow(U Z)$)
#let VA = math.class("normal", $arrow(V A)$)
#let VB = math.class("normal", $arrow(V B)$)
#let VC = math.class("normal", $arrow(V C)$)
#let VD = math.class("normal", $arrow(V D)$)
#let VE = math.class("normal", $arrow(V E)$)
#let VF = math.class("normal", $arrow(V F)$)
#let VG = math.class("normal", $arrow(V G)$)
#let VH = math.class("normal", $arrow(V H)$)
#let VI = math.class("normal", $arrow(V I)$)
#let VJ = math.class("normal", $arrow(V J)$)
#let VK = math.class("normal", $arrow(V K)$)
#let VL = math.class("normal", $arrow(V L)$)
#let VM = math.class("normal", $arrow(V M)$)
#let VN = math.class("normal", $arrow(V N)$)
#let VO = math.class("normal", $arrow(V O)$)
#let VP = math.class("normal", $arrow(V P)$)
#let VQ = math.class("normal", $arrow(V Q)$)
#let VR = math.class("normal", $arrow(V R)$)
#let VS = math.class("normal", $arrow(V S)$)
#let VT = math.class("normal", $arrow(V T)$)
#let VU = math.class("normal", $arrow(V U)$)
#let VW = math.class("normal", $arrow(V W)$)
#let VX = math.class("normal", $arrow(V X)$)
#let VY = math.class("normal", $arrow(V Y)$)
#let VZ = math.class("normal", $arrow(V Z)$)
#let WA = math.class("normal", $arrow(W A)$)
#let WB = math.class("normal", $arrow(W B)$)
#let WC = math.class("normal", $arrow(W C)$)
#let WD = math.class("normal", $arrow(W D)$)
#let WE = math.class("normal", $arrow(W E)$)
#let WF = math.class("normal", $arrow(W F)$)
#let WG = math.class("normal", $arrow(W G)$)
#let WH = math.class("normal", $arrow(W H)$)
#let WI = math.class("normal", $arrow(W I)$)
#let WJ = math.class("normal", $arrow(W J)$)
#let WK = math.class("normal", $arrow(W K)$)
#let WL = math.class("normal", $arrow(W L)$)
#let WM = math.class("normal", $arrow(W M)$)
#let WN = math.class("normal", $arrow(W N)$)
#let WO = math.class("normal", $arrow(W O)$)
#let WP = math.class("normal", $arrow(W P)$)
#let WQ = math.class("normal", $arrow(W Q)$)
#let WR = math.class("normal", $arrow(W R)$)
#let WS = math.class("normal", $arrow(W S)$)
#let WT = math.class("normal", $arrow(W T)$)
#let WU = math.class("normal", $arrow(W U)$)
#let WV = math.class("normal", $arrow(W V)$)
#let WX = math.class("normal", $arrow(W X)$)
#let WY = math.class("normal", $arrow(W Y)$)
#let WZ = math.class("normal", $arrow(W Z)$)
#let XA = math.class("normal", $arrow(X A)$)
#let XB = math.class("normal", $arrow(X B)$)
#let XC = math.class("normal", $arrow(X C)$)
#let XD = math.class("normal", $arrow(X D)$)
#let XE = math.class("normal", $arrow(X E)$)
#let XF = math.class("normal", $arrow(X F)$)
#let XG = math.class("normal", $arrow(X G)$)
#let XH = math.class("normal", $arrow(X H)$)
#let XI = math.class("normal", $arrow(X I)$)
#let XJ = math.class("normal", $arrow(X J)$)
#let XK = math.class("normal", $arrow(X K)$)
#let XL = math.class("normal", $arrow(X L)$)
#let XM = math.class("normal", $arrow(X M)$)
#let XN = math.class("normal", $arrow(X N)$)
#let XO = math.class("normal", $arrow(X O)$)
#let XP = math.class("normal", $arrow(X P)$)
#let XQ = math.class("normal", $arrow(X Q)$)
#let XR = math.class("normal", $arrow(X R)$)
#let XS = math.class("normal", $arrow(X S)$)
#let XT = math.class("normal", $arrow(X T)$)
#let XU = math.class("normal", $arrow(X U)$)
#let XV = math.class("normal", $arrow(X V)$)
#let XW = math.class("normal", $arrow(X W)$)
#let XY = math.class("normal", $arrow(X Y)$)
#let XZ = math.class("normal", $arrow(X Z)$)
#let YA = math.class("normal", $arrow(Y A)$)
#let YB = math.class("normal", $arrow(Y B)$)
#let YC = math.class("normal", $arrow(Y C)$)
#let YD = math.class("normal", $arrow(Y D)$)
#let YE = math.class("normal", $arrow(Y E)$)
#let YF = math.class("normal", $arrow(Y F)$)
#let YG = math.class("normal", $arrow(Y G)$)
#let YH = math.class("normal", $arrow(Y H)$)
#let YI = math.class("normal", $arrow(Y I)$)
#let YJ = math.class("normal", $arrow(Y J)$)
#let YK = math.class("normal", $arrow(Y K)$)
#let YL = math.class("normal", $arrow(Y L)$)
#let YM = math.class("normal", $arrow(Y M)$)
#let YN = math.class("normal", $arrow(Y N)$)
#let YO = math.class("normal", $arrow(Y O)$)
#let YP = math.class("normal", $arrow(Y P)$)
#let YQ = math.class("normal", $arrow(Y Q)$)
#let YR = math.class("normal", $arrow(Y R)$)
#let YS = math.class("normal", $arrow(Y S)$)
#let YT = math.class("normal", $arrow(Y T)$)
#let YU = math.class("normal", $arrow(Y U)$)
#let YV = math.class("normal", $arrow(Y V)$)
#let YW = math.class("normal", $arrow(Y W)$)
#let YX = math.class("normal", $arrow(Y X)$)
#let YZ = math.class("normal", $arrow(Y Z)$)
#let ZA = math.class("normal", $arrow(Z A)$)
#let ZB = math.class("normal", $arrow(Z B)$)
#let ZC = math.class("normal", $arrow(Z C)$)
#let ZD = math.class("normal", $arrow(Z D)$)
#let ZE = math.class("normal", $arrow(Z E)$)
#let ZF = math.class("normal", $arrow(Z F)$)
#let ZG = math.class("normal", $arrow(Z G)$)
#let ZH = math.class("normal", $arrow(Z H)$)
#let ZI = math.class("normal", $arrow(Z I)$)
#let ZJ = math.class("normal", $arrow(Z J)$)
#let ZK = math.class("normal", $arrow(Z K)$)
#let ZL = math.class("normal", $arrow(Z L)$)
#let ZM = math.class("normal", $arrow(Z M)$)
#let ZN = math.class("normal", $arrow(Z N)$)
#let ZO = math.class("normal", $arrow(Z O)$)
#let ZP = math.class("normal", $arrow(Z P)$)
#let ZQ = math.class("normal", $arrow(Z Q)$)
#let ZR = math.class("normal", $arrow(Z R)$)
#let ZS = math.class("normal", $arrow(Z S)$)
#let ZT = math.class("normal", $arrow(Z T)$)
#let ZU = math.class("normal", $arrow(Z U)$)
#let ZV = math.class("normal", $arrow(Z V)$)
#let ZW = math.class("normal", $arrow(Z W)$)
#let ZX = math.class("normal", $arrow(Z X)$)
#let ZY = math.class("normal", $arrow(Z Y)$)
